<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.client">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".CustomerOrderSummaryActivity"></activity>
        <activity android:name=".CustomerItemClickActivity" />
        <activity android:name=".ShopViewOrdersActivity" />
        <activity android:name=".CustomerViewItemsActivity" />
        <activity android:name=".CustomerViewOrdersActivity" />
        <activity android:name=".ShopViewItemsActivity" />
        <activity android:name=".CustomerViewShopsActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>